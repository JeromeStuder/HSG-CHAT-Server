import { Observable } from 'rxjs/Observable';
import { Subject } from 'rxjs/Subject';
export declare class RxSocket<DT> {
    private socket;
    constructor(url: string, options?: SocketIOClient.ConnectOpts);
    disconnect(): void;
    connect(): void;
    observable<T = DT>(event: string): Observable<T>;
    subject<T = DT>(event: string): Subject<T>;
    private createEventSubject<T>(event);
    private createEventObservable<T>(event);
    private onEventSubjectUnsubscribe(event);
}
